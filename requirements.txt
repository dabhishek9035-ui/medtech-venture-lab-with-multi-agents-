# Core LangGraph + LangChain
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-google-genai>=2.0.0

# Google Gemini SDK (langchain-google-genai depends on this)
google-generativeai>=0.8.0

# Environment variable management
python-dotenv>=1.0.0

# Utilities
pydantic>=2.0.0
tenacity>=8.2.0        # retry logic for rate-limited API calls
rich>=13.0.0           # pretty terminal output (optional but useful)